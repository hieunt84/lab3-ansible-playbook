---
- name: play1 
  hosts: centos 
  tasks:
    - name: Check version of Apache.
      command: httpd "-v"
      changed_when: false
      check_mode: false
      register: result
    - debug:
        msg: "{{ result.stdout}}"